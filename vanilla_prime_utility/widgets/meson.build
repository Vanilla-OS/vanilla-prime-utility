pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
widgetsdir = join_paths(pkgdatadir, 'vanilla_prime_utility/widgets')

sources = [
  '__init__.py',
  'profile.py',
]

install_data(sources, install_dir: widgetsdir)